import {StyleSheet} from 'react-native';

export const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
  },
  flatListItem: {
    padding: 10,
    fontSize: 18,
    height: 44,
  }
});
